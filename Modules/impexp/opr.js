

// const add = (a,b) =>{
// return a+b;



// };

// module.exports = add;//to export a single module.

// const add = (a,b)=>{

// return a+b;

// };
// const sub = (c,d)=>{

//     return c-d;
    
//  };

//  module.exports.add=add;equal to ke bad vahi name jo function ka name hai and equal to se pehle koi bhi name can be inserted. vahi value function je name ki jagah index par ayegi then.
//  module.exports.sub=sub;  when calling two functions this is how we export them.

// module.exports = {add,sub,mult};//when exporting more functions we can destructure them this way and can export them.
//we can also export the variables also just type in the name of the variable beside the function name in destructuring..
//eg ==> module.exports = {add,sub, name};


    