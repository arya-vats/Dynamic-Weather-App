// const opr = require("./opr");//when calling the module with only one function in it.
// console.log(add(5,5));//when calling a single function.

// console.log(opr.add(5,5));
// console.log((opr.sub(10,5)));//when calling two functions we call them as if they are a part of an object.

// const {add, sub} = require("./opr");
// console.log(add(10,5));
// console.log(sub(5,2));//we can also call the two or more functions by destructuring them and calling them directly.